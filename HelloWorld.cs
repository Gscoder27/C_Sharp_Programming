using System;

namespace HelloWorldApp
{
    class HelloWorld{
        static void Main(string[] args) {
            // for printing we use
            Console.WriteLine("Hello World !!");
            Console.ReadKey(); // to hold the console window - Waits for the user to press a key so that the console window doesn't close immediately after printing 
        }
    }
}